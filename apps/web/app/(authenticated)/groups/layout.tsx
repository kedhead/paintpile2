import { GroupLayout } from '../../../components/groups/group-layout';

export default function GroupsLayout({ children }: { children: React.ReactNode }) {
  return <GroupLayout>{children}</GroupLayout>;
}
